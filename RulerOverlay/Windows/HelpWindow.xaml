<Window x:Class="RulerOverlay.Windows.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Help &amp; Keyboard Shortcuts"
        Width="700" Height="600"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False">

    <Window.Resources>
        <!-- Keyboard Key Style -->
        <Style x:Key="KeyStyle" TargetType="Border">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="Margin" Value="2"/>
        </Style>

        <Style x:Key="KeyTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Consolas, Courier New"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,15,0,10"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="20,15">
            <TextBlock Text="Help &amp; Keyboard Shortcuts"
                       Foreground="White"
                       FontSize="20"
                       FontWeight="Bold"/>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel>
                <!-- Keyboard Shortcuts Section -->
                <TextBlock Text="Keyboard Shortcuts" Style="{StaticResource SectionTitleStyle}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Ctrl+R -->
                    <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="0,3">
                        <Border Style="{StaticResource KeyStyle}">
                            <TextBlock Text="Ctrl" Style="{StaticResource KeyTextStyle}"/>
                        </Border>
                        <TextBlock Text="+" VerticalAlignment="Center" Margin="3,0"/>
                        <Border Style="{StaticResource KeyStyle}">
                            <TextBlock Text="R" Style="{StaticResource KeyTextStyle}"/>
                        </Border>
                    </StackPanel>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="Reset ruler position to center" VerticalAlignment="Center" Margin="10,0"/>

                    <!-- Ctrl+C -->
                    <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Margin="0,3">
                        <Border Style="{StaticResource KeyStyle}">
                            <TextBlock Text="Ctrl" Style="{StaticResource KeyTextStyle}"/>
                        </Border>
                        <TextBlock Text="+" VerticalAlignment="Center" Margin="3,0"/>
                        <Border Style="{StaticResource KeyStyle}">
                            <TextBlock Text="C" Style="{StaticResource KeyTextStyle}"/>
                        </Border>
                    </StackPanel>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="Copy current measurement to clipboard" VerticalAlignment="Center" Margin="10,0"/>

                    <!-- Ctrl+T -->
                    <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal" Margin="0,3">
                        <Border Style="{StaticResource KeyStyle}">
                            <TextBlock Text="Ctrl" Style="{StaticResource KeyTextStyle}"/>
                        </Border>
                        <TextBlock Text="+" VerticalAlignment="Center" Margin="3,0"/>
                        <Border Style="{StaticResource KeyStyle}">
                            <TextBlock Text="T" Style="{StaticResource KeyTextStyle}"/>
                        </Border>
                    </StackPanel>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="Toggle transparency" VerticalAlignment="Center" Margin="10,0"/>

                    <!-- Ctrl+M -->
                    <StackPanel Grid.Row="3" Grid.Column="0" Orientation="Horizontal" Margin="0,3">
                        <Border Style="{StaticResource KeyStyle}">
                            <TextBlock Text="Ctrl" Style="{StaticResource KeyTextStyle}"/>
                        </Border>
                        <TextBlock Text="+" VerticalAlignment="Center" Margin="3,0"/>
                        <Border Style="{StaticResource KeyStyle}">
                            <TextBlock Text="M" Style="{StaticResource KeyTextStyle}"/>
                        </Border>
                    </StackPanel>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="Toggle magnifier" VerticalAlignment="Center" Margin="10,0"/>

                    <!-- Ctrl+S -->
                    <StackPanel Grid.Row="4" Grid.Column="0" Orientation="Horizontal" Margin="0,3">
                        <Border Style="{StaticResource KeyStyle}">
                            <TextBlock Text="Ctrl" Style="{StaticResource KeyTextStyle}"/>
                        </Border>
                        <TextBlock Text="+" VerticalAlignment="Center" Margin="3,0"/>
                        <Border Style="{StaticResource KeyStyle}">
                            <TextBlock Text="S" Style="{StaticResource KeyTextStyle}"/>
                        </Border>
                    </StackPanel>
                    <TextBlock Grid.Row="4" Grid.Column="1" Text="Toggle edge snapping" VerticalAlignment="Center" Margin="10,0"/>

                    <!-- Ctrl+P -->
                    <StackPanel Grid.Row="5" Grid.Column="0" Orientation="Horizontal" Margin="0,3">
                        <Border Style="{StaticResource KeyStyle}">
                            <TextBlock Text="Ctrl" Style="{StaticResource KeyTextStyle}"/>
                        </Border>
                        <TextBlock Text="+" VerticalAlignment="Center" Margin="3,0"/>
                        <Border Style="{StaticResource KeyStyle}">
                            <TextBlock Text="P" Style="{StaticResource KeyTextStyle}"/>
                        </Border>
                    </StackPanel>
                    <TextBlock Grid.Row="5" Grid.Column="1" Text="Enter/exit point-to-point mode" VerticalAlignment="Center" Margin="10,0"/>

                    <!-- Ctrl+Q -->
                    <StackPanel Grid.Row="6" Grid.Column="0" Orientation="Horizontal" Margin="0,3">
                        <Border Style="{StaticResource KeyStyle}">
                            <TextBlock Text="Ctrl" Style="{StaticResource KeyTextStyle}"/>
                        </Border>
                        <TextBlock Text="+" VerticalAlignment="Center" Margin="3,0"/>
                        <Border Style="{StaticResource KeyStyle}">
                            <TextBlock Text="Q" Style="{StaticResource KeyTextStyle}"/>
                        </Border>
                    </StackPanel>
                    <TextBlock Grid.Row="6" Grid.Column="1" Text="Quit application" VerticalAlignment="Center" Margin="10,0"/>

                    <!-- Ctrl+G -->
                    <StackPanel Grid.Row="7" Grid.Column="0" Orientation="Horizontal" Margin="0,3">
                        <Border Style="{StaticResource KeyStyle}">
                            <TextBlock Text="Ctrl" Style="{StaticResource KeyTextStyle}"/>
                        </Border>
                        <TextBlock Text="+" VerticalAlignment="Center" Margin="3,0"/>
                        <Border Style="{StaticResource KeyStyle}">
                            <TextBlock Text="G" Style="{StaticResource KeyTextStyle}"/>
                        </Border>
                    </StackPanel>
                    <TextBlock Grid.Row="7" Grid.Column="1" Text="Clear all edge guides" VerticalAlignment="Center" Margin="10,0"/>

                    <!-- F1 -->
                    <StackPanel Grid.Row="8" Grid.Column="0" Orientation="Horizontal" Margin="0,3">
                        <Border Style="{StaticResource KeyStyle}">
                            <TextBlock Text="F1" Style="{StaticResource KeyTextStyle}"/>
                        </Border>
                    </StackPanel>
                    <TextBlock Grid.Row="8" Grid.Column="1" Text="Show this help dialog" VerticalAlignment="Center" Margin="10,0"/>

                    <!-- Esc -->
                    <StackPanel Grid.Row="9" Grid.Column="0" Orientation="Horizontal" Margin="0,3">
                        <Border Style="{StaticResource KeyStyle}">
                            <TextBlock Text="Esc" Style="{StaticResource KeyTextStyle}"/>
                        </Border>
                    </StackPanel>
                    <TextBlock Grid.Row="9" Grid.Column="1" Text="Exit point-to-point mode or close dialogs" VerticalAlignment="Center" Margin="10,0"/>
                </Grid>

                <!-- Using the Ruler Section -->
                <TextBlock Text="Using the Ruler" Style="{StaticResource SectionTitleStyle}"/>
                <TextBlock TextWrapping="Wrap" Margin="0,5">
                    <Run Text="• " FontWeight="Bold"/>
                    <Run Text="Move: " FontWeight="Bold"/>
                    <Run Text="Click and drag anywhere on the ruler"/>
                    <LineBreak/>
                    <Run Text="• " FontWeight="Bold"/>
                    <Run Text="Resize: " FontWeight="Bold"/>
                    <Run Text="Drag from left or right edges"/>
                    <LineBreak/>
                    <Run Text="• " FontWeight="Bold"/>
                    <Run Text="Rotate: " FontWeight="Bold"/>
                    <Run Text="Right-click for preset angles (0°, 45°, 90°, 135°, 180°)"/>
                    <LineBreak/>
                    <Run Text="• " FontWeight="Bold"/>
                    <Run Text="Change Unit: " FontWeight="Bold"/>
                    <Run Text="Right-click → Measurement Unit"/>
                    <LineBreak/>
                    <Run Text="• " FontWeight="Bold"/>
                    <Run Text="Transparency: " FontWeight="Bold"/>
                    <Run Text="Right-click → Transparency"/>
                    <LineBreak/>
                    <Run Text="• " FontWeight="Bold"/>
                    <Run Text="Colors: " FontWeight="Bold"/>
                    <Run Text="Right-click → Color"/>
                </TextBlock>

                <!-- Point-to-Point Measurement Section -->
                <TextBlock Text="Point-to-Point Measurement" Style="{StaticResource SectionTitleStyle}"/>
                <TextBlock TextWrapping="Wrap" Margin="0,5">
                    <Run Text="• Press Ctrl+P or right-click → Point-to-Point Mode"/>
                    <LineBreak/>
                    <Run Text="• Click and drag to create a measurement line"/>
                    <LineBreak/>
                    <Run Text="• Distance is shown in current unit while dragging"/>
                    <LineBreak/>
                    <Run Text="• Press Esc to exit point-to-point mode"/>
                </TextBlock>

                <!-- Edge Guides Section -->
                <TextBlock Text="Edge Guides" Style="{StaticResource SectionTitleStyle}"/>
                <TextBlock TextWrapping="Wrap" Margin="0,5">
                    <Run Text="• Hold Shift and click inside the ruler to create magenta guide lines"/>
                    <LineBreak/>
                    <Run Text="• Shift+click on an existing guide to remove it individually"/>
                    <LineBreak/>
                    <Run Text="• Press Ctrl+G to clear all guides at once"/>
                    <LineBreak/>
                    <Run Text="• Right-click → Clear All Guides also removes all guides"/>
                </TextBlock>

                <!-- Magnifier Section -->
                <TextBlock Text="Magnifier" Style="{StaticResource SectionTitleStyle}"/>
                <TextBlock TextWrapping="Wrap" Margin="0,5">
                    <Run Text="• Move cursor within 3 pixels of any ruler corner"/>
                    <LineBreak/>
                    <Run Text="• Magnifier appears showing 4x zoomed view"/>
                    <LineBreak/>
                    <Run Text="• Press Ctrl+M to toggle magnifier on/off"/>
                    <LineBreak/>
                    <Run Text="• Right-click → Enable/Disable Magnifier"/>
                </TextBlock>

                <!-- Calibration Section -->
                <TextBlock Text="Calibration" Style="{StaticResource SectionTitleStyle}"/>
                <TextBlock TextWrapping="Wrap" Margin="0,5">
                    <Run Text="• " FontWeight="Bold"/>
                    <Run Text="Why Calibrate? " FontWeight="Bold"/>
                    <Run Text="Ensures accurate inch and centimeter measurements"/>
                    <LineBreak/>
                    <Run Text="• " FontWeight="Bold"/>
                    <Run Text="How to Calibrate: " FontWeight="Bold"/>
                    <Run Text="Right-click → Calibrate Screen"/>
                    <LineBreak/>
                    <Run Text="• " FontWeight="Bold"/>
                    <Run Text="Enter Your Screen Diagonal: " FontWeight="Bold"/>
                    <Run Text="Input the diagonal size in inches (e.g., 15.6&quot;, 27&quot;)"/>
                    <LineBreak/>
                    <Run Text="• " FontWeight="Bold"/>
                    <Run Text="Common Sizes: " FontWeight="Bold"/>
                    <Run Text="Laptops: 13.3&quot;-15.6&quot;, Monitors: 24&quot;-27&quot;"/>
                </TextBlock>

                <!-- Clipboard Integration Section -->
                <TextBlock Text="Clipboard Integration" Style="{StaticResource SectionTitleStyle}"/>
                <TextBlock TextWrapping="Wrap" Margin="0,5">
                    <Run Text="• Press Ctrl+C to copy current measurement"/>
                    <LineBreak/>
                    <Run Text="• Right-click → Copy Measurement"/>
                    <LineBreak/>
                    <Run Text="• Measurements are copied in current unit (e.g., &quot;500px&quot;, &quot;5.21 in&quot;, &quot;13.23 cm&quot;)"/>
                    <LineBreak/>
                    <Run Text="• Toast notification confirms successful copy"/>
                </TextBlock>

                <!-- Troubleshooting Section -->
                <TextBlock Text="Troubleshooting" Style="{StaticResource SectionTitleStyle}"/>
                <TextBlock TextWrapping="Wrap" Margin="0,5,0,20">
                    <Run Text="• " FontWeight="Bold"/>
                    <Run Text="Ruler not visible: " FontWeight="Bold"/>
                    <Run Text="Press Ctrl+R to reset to center screen"/>
                    <LineBreak/>
                    <Run Text="• " FontWeight="Bold"/>
                    <Run Text="Measurements inaccurate (inches/cm): " FontWeight="Bold"/>
                    <Run Text="Calibrate your screen for accurate PPI"/>
                    <LineBreak/>
                    <Run Text="• " FontWeight="Bold"/>
                    <Run Text="Can't move ruler: " FontWeight="Bold"/>
                    <Run Text="Ensure you're not in point-to-point mode (press Esc)"/>
                    <LineBreak/>
                    <Run Text="• " FontWeight="Bold"/>
                    <Run Text="Magnifier not appearing: " FontWeight="Bold"/>
                    <Run Text="Move cursor within 3 pixels of a corner; check if toggle is enabled"/>
                </TextBlock>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#F5F5F5" BorderBrush="#DDDDDD" BorderThickness="0,1,0,0" Padding="15">
            <Button Content="Close"
                    Width="100"
                    Height="30"
                    HorizontalAlignment="Right"
                    Click="CloseButton_Click"/>
        </Border>
    </Grid>
</Window>
