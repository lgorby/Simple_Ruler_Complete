<Window x:Class="RulerOverlay.Windows.PointToPointWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Point-to-Point Measurement"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowState="Maximized"
        Cursor="Cross"
        MouseDown="Window_MouseDown"
        MouseMove="Window_MouseMove"
        MouseUp="Window_MouseUp"
        KeyDown="Window_KeyDown">

    <Grid>
        <!-- Semi-transparent background overlay -->
        <Rectangle Fill="Black" Opacity="0.15"/>

        <!-- Measurement Line Canvas -->
        <Canvas x:Name="MeasurementCanvas">
            <!-- Measurement line will be drawn here in code-behind -->
        </Canvas>

        <!-- Instructions Overlay -->
        <Border VerticalAlignment="Top"
                HorizontalAlignment="Center"
                Margin="0,20,0,0"
                Background="#CC000000"
                Padding="20,10"
                CornerRadius="3">
            <StackPanel>
                <TextBlock Text="Point-to-Point Measurement Mode"
                           Foreground="White"
                           FontSize="16"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Click and drag to measure distance between two points"
                           Foreground="White"
                           FontSize="12"
                           Margin="0,5,0,0"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Press Esc to exit"
                           Foreground="#FFDDDDDD"
                           FontSize="11"
                           Margin="0,5,0,0"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
